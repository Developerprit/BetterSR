<Window x:Class="BetterSR.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BetterSR"
        Height="580" Width="760"
        MinHeight="540" MinWidth="680"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBg}"
        Foreground="{DynamicResource WindowFg}"
        FontFamily="Microsoft YaHei, Segoe UI"
        Icon="Assets/Background.png">

    <Window.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="18"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="0" BlurRadius="12" Opacity="0.15" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MainButton" TargetType="Button">
            <Setter Property="Height" Value="110"/>
            <Setter Property="Width" Value="170"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonFg}"/>
            <Setter Property="Background" Value="{DynamicResource Accent}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="16"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentHover}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="IconTextBlock" TargetType="TextBlock">
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ToolButton" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Width" Value="34"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentLight}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AudioToggle" TargetType="ToggleButton">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border CornerRadius="10" Background="{TemplateBinding Background}" Padding="10,4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Accent}"/>
                    <Setter Property="Foreground" Value="{DynamicResource ButtonFg}"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="False">
                    <Setter Property="Background" Value="{DynamicResource ToggleOff}"/>
                    <Setter Property="Foreground" Value="{DynamicResource WindowFg}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Image Source="Assets/Background.png" Stretch="UniformToFill" Opacity="0.5" Panel.ZIndex="-1"/>

        <Border Style="{StaticResource CardStyle}" Margin="24" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="BetterSR" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource Accent}"/>
                        <TextBlock Text="  更好的录制屏幕" FontSize="14" VerticalAlignment="Bottom" Margin="8,0,0,4" Foreground="{DynamicResource StatusFg}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="ThemeButton" Style="{StaticResource ToolButton}" Content="🌙" Click="ThemeButton_Click" Margin="4,0"/>
                        <Button x:Name="HelpButton" Style="{StaticResource ToolButton}" Content="?" Click="HelpButton_Click" Margin="4,0"/>
                        <Button x:Name="SettingsButton" Style="{StaticResource ToolButton}" Content="⚙" Click="SettingsButton_Click" Margin="4,0"/>
                    </StackPanel>
                </Grid>

                <!-- Saying -->
                <Border Grid.Row="1" Background="{DynamicResource AccentLight}" CornerRadius="12" Margin="0,16,0,0" Padding="14,10">
                    <TextBlock x:Name="SayingText" Text="正在获取一言..." FontSize="14" Foreground="{DynamicResource SayingFg}" TextWrapping="Wrap" TextAlignment="Center"/>
                </Border>

                <!-- Main controls -->
                <Grid Grid.Row="2" VerticalAlignment="Center" Margin="0,24,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="BtnFullscreen" Style="{StaticResource MainButton}" Click="BtnFullscreen_Click" Margin="12">
                            <StackPanel>
                                <TextBlock Text="🖥" Style="{StaticResource IconTextBlock}"/>
                                <TextBlock Text="全屏录制" Margin="0,6,0,0"/>
                                <TextBlock Text="Ctrl+Alt+F9" FontSize="11" Opacity="0.8" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="BtnRegion" Style="{StaticResource MainButton}" Click="BtnRegion_Click" Margin="12">
                            <StackPanel>
                                <TextBlock Text="⬚" Style="{StaticResource IconTextBlock}"/>
                                <TextBlock Text="区域录制" Margin="0,6,0,0"/>
                                <TextBlock Text="Ctrl+Alt+R" FontSize="11" Opacity="0.8" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="BtnWindow" Style="{StaticResource MainButton}" Click="BtnWindow_Click" Margin="12">
                            <StackPanel>
                                <TextBlock Text="🪟" Style="{StaticResource IconTextBlock}"/>
                                <TextBlock Text="窗口录制" Margin="0,6,0,0"/>
                                <TextBlock Text="Ctrl+Alt+W" FontSize="11" Opacity="0.8" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                        <ToggleButton x:Name="ToggleSystemAudio" Style="{StaticResource AudioToggle}" Content="🔊 系统音频" Margin="8" Checked="ToggleSystemAudio_Checked" Unchecked="ToggleSystemAudio_Unchecked"/>
                        <ToggleButton x:Name="ToggleMicrophone" Style="{StaticResource AudioToggle}" Content="🎤 麦克风" Margin="8" Checked="ToggleMicrophone_Checked" Unchecked="ToggleMicrophone_Unchecked"/>
                    </StackPanel>
                </Grid>

                <!-- Recording status -->
                <Grid Grid.Row="3" Margin="0,20,0,0" Visibility="Collapsed" x:Name="RecordingPanel">
                    <Border Background="{DynamicResource CardBg}" CornerRadius="14" Padding="16,10" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Ellipse Grid.Column="0" Width="14" Height="14" Fill="{DynamicResource RecordingRed}" Margin="0,0,10,0">
                                <Ellipse.Triggers>
                                    <EventTrigger RoutedEvent="Ellipse.Loaded">
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0.3" Duration="0:0:0.8" AutoReverse="True"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Ellipse.Triggers>
                            </Ellipse>
                            <TextBlock Grid.Column="1" x:Name="StatusText" Text="正在录制" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                <TextBlock x:Name="DurationText" Text="00:00:00" VerticalAlignment="Center" FontSize="16" FontWeight="Bold" Margin="0,0,16,0"/>
                                <Button x:Name="BtnPause" Style="{StaticResource ToolButton}" Content="⏸" Click="BtnPause_Click" Margin="2,0"/>
                                <Button x:Name="BtnStop" Style="{StaticResource ToolButton}" Content="⏹" Click="BtnStop_Click" Margin="2,0"/>
                                <Button x:Name="BtnDiscard" Style="{StaticResource ToolButton}" Content="🗑" Click="BtnDiscard_Click" Margin="2,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Footer -->
                <Grid Grid.Row="4" Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" x:Name="FooterText" Text="就绪" FontSize="12" Foreground="{DynamicResource StatusFg}"/>
                    <TextBlock Grid.Column="1" Text="BetterSR v1.0.0" FontSize="12" Foreground="{DynamicResource StatusFg}" Opacity="0.7"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
